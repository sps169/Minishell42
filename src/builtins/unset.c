#include "minishell.h"

char **loop_delete_var(char **arr, char **aux, char *str)
{
  int i;
  int j;

  i = 0;
  j = 0;

  while (arr[i] != NULL)
  {
    if (!(ft_strncmp(arr[i], str, after)))
  }
}
